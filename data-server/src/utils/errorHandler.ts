
// basic error handler just to get a single handler everywhere
// => hash out the logic later for proper handler
const handleError = (error: any): void => {
    console.log(error);
    return;
};


export default handleError;